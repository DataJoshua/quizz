.card-wrapper
  .card-title
    = quiz.name

  - if allowed_to?(:create?, quiz, with: AnswerPolicy) || quiz.discarded?
    = render "students/dashboards/button", label: "Solve", quiz: quiz
  - else
    = render "students/dashboards/button", label: "View Results", quiz: quiz
